#include <stdio.h>
int main()
{
int dividend,divisor,division;
printf("the value of dividend");
scanf("%d",&dividend);
printf("the value of divisor");
scanf("%d",&divisor);
division=dividend/divisor;
printf("the value of division is %d",division);
return 7;
}